import 'package:flutter/material.dart';

class IngredientToRecipe extends StatelessWidget {
  const IngredientToRecipe({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}